import * as vscode from "vscode"
/**
 * Checks for launch configuration and runs the task immediately if found.
 * Reads .kilocode/launchConfig.json from the workspace root.
 */
export declare function checkAndRunAutoLaunchingTask(context: vscode.ExtensionContext): Promise<void>
//# sourceMappingURL=autoLaunchingTask.d.ts.map
